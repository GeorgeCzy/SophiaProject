Each JSON file in this folder defines one experiment version.

The active study versions are listed in `../experiments.json`. The default
public URL uses `complete.json`; other versions are selected with
`?experiment=<id>`.
